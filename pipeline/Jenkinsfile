pipeline {
    agent any
    triggers {
        pollSCM 'H/15 * * * *'
    }
    stages {
        stage("Determine products and tests to run.") {
            steps {
                sh "ls -al"
                sh "test making some changes."
            }
        }
    }
}